{
  "name": "camp-gallery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c red,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "check": "npm --prefix backend run check && npm --prefix frontend run check",
    "start:api": "npm --prefix backend start",
    "start:web": "npm --prefix frontend run preview"
  },
  "devDependencies": { "concurrently": "^9.2.0" }
}
